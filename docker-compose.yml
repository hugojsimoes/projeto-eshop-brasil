version: '3.8'
services:
  mongodb:
    image: mongo
    container_name: mongodb
    ports:
      - "27017:27017"
  streamlit:
    build: .
    ports:
      - "8501:8501"
    depends_on:
      - mongodb
